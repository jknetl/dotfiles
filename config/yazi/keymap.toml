# Scaffold only — no keybindings overridden, defaults apply.
# Use [[mgr.prepend_keymap]] / [[mgr.append_keymap]] to add bindings without
# replacing defaults. See https://yazi-rs.github.io/docs/configuration/keymap
